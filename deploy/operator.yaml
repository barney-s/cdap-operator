apiVersion: apps/v1
kind: Deployment
metadata:
  name: cdap-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      name: cdap-operator
  template:
    metadata:
      labels:
        name: cdap-operator
    spec:
      serviceAccountName: cdap-operator
      containers:
        - name: cdap-operator
          # Replace this with the built image name
          image: gcr.io/cdap-alpha-dev/cdap-operator
          ports:
          - containerPort: 60000
            name: metrics
          command:
          - cdap-operator
          imagePullPolicy: Always
          readinessProbe:
            exec:
              command:
                - stat
                - /tmp/operator-sdk-ready
            initialDelaySeconds: 4
            periodSeconds: 10
            failureThreshold: 1
          env:
            - name: WATCH_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: OPERATOR_NAME
              value: "cdap-operator"
